import css from './Header.module.css';

const Header = () => {
  return (
    <div className={css.header}>
      Header Headre
      <button> ВИЙТИ</button>
    </div>
  );
};

export default Header;
